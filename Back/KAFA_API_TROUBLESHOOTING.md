# KAFA API 문제 해결 가이드

## 🔧 API 404 에러 문제 해결

### 문제 상황
- 새로 추가된 KAFA 크롤링 API 엔드포인트들이 404 에러 발생
- 컨트롤러에 엔드포인트가 정상적으로 추가되어 있음에도 불구하고 서버가 인식하지 못함

### 원인 분석
1. **백엔드 서버 실행 포트 문제**
   - 프론트엔드가 3000번 포트 사용
   - 백엔드가 4000번 포트에서 실행됨
   - API 경로 prefix `/api` 필요

2. **서버 재시작 후에도 404 에러 지속**
   - 컨트롤러 문법 에러는 없음 (`npm run build` 성공)
   - 모듈 등록도 정상 (`KafaStatsModule` 임포트됨)

### ✅ 해결 방법
```bash
# 올바른 API 호출 방법
curl -X POST "http://localhost:4000/api/kafa-stats/엔드포인트명"

# 잘못된 방법들
curl -X POST "http://localhost:3000/kafa-stats/엔드포인트명"  # 프론트엔드 포트
curl -X POST "http://localhost:4000/kafa-stats/엔드포인트명"  # /api prefix 누락
```

### 📋 체크리스트
1. **백엔드 서버 실행 확인**: `cd Back && npm run start:dev`
2. **포트 확인**: 4000번 포트에서 백엔드 실행되는지 확인
3. **API prefix**: `/api` 경로 포함하여 호출
4. **컨트롤러 문법**: `npm run build`로 TypeScript 에러 확인
5. **모듈 등록**: `app.module.ts`에서 모듈 임포트 확인

---

## 🏈 인터셉션 값 오류 문제

### 문제 상황
- 인터셉션 크롤링에서 실제 값이 있는 선수들이 0으로 표시됨
- 예: 양병욱(서울시립대) 실제 5개 인터셉션 → 크롤링 결과 0개
- 태클은 정상 작동하므로 크롤링 구조 문제가 아님

### 원인 (추정)
1. **HTML 테이블 컬럼 순서 불일치**
   - KAFA 웹사이트에서 인터셉션 페이지의 컬럼 구조가 예상과 다름
   - 셀 인덱스 매핑이 잘못되어 엉뚱한 컬럼값을 파싱

2. **정렬별 페이지 구조 차이**
   - 인터셉션의 각 정렬 방식(INT, YDS, TD, LNG)별로 컬럼 순서가 다를 수 있음
   - 일부 정렬에서만 올바른 값이 나오는 상황 가능

### 🔍 현재 상태 (미해결)
- **해결됨**: 킥킹/펀팅 스탯 (필드골, 킥오프, 펀트, 킥오프리턴, 펀트리턴)
- **문제있음**: 인터셉션 스탯의 값 파싱 오류
- **정상 작동**: 태클 스탯 (참고용 정상 패턴)

### 🛠️ 해결 필요 작업
1. **태클 vs 인터셉션 비교 분석**
   - 태클 크롤링 메서드와 인터셉션 크롤링 메서드 구조 비교
   - HTML 파싱 부분의 셀 인덱스 차이점 확인

2. **KAFA 웹사이트 구조 분석**
   - 인터셉션 페이지의 실제 HTML 구조 확인
   - 각 정렬별 컬럼 순서 매핑 재검토

3. **파싱 로직 수정**
   - 올바른 셀 인덱스로 값 추출
   - 태클과 동일한 패턴으로 수정

### 📝 참고사항
- 사용자 요청: "태클 부분 보고 학습을 하고 양식 맞춰서 인터셉션부터 다시 짜봐"
- 태클 구현이 정상 작동하므로 이를 기준으로 인터셉션 수정 필요